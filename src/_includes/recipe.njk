<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ title }} – Taste Tales Lieblingsrezepte.">
  <meta name="author" content="Steve Minder und Florian Zingg">
  <title>{{ title }} – Taste Tales</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body>
  <header>
    <h1>{{ title }} {{ emoji }}</h1>
    <nav>
      <a href="{{ '/' | url }}">Startseite</a>
      <a href="{{ '/vorspeisen/' | url }}">Vorspeisen</a>
      <a href="{{ '/hauptgerichte/' | url }}">Hauptgerichte</a>
      <a href="{{ '/nachtische/' | url }}">Nachtische</a>
      <a href="{{ '/gebaeck/' | url }}">Gebäck</a>
      <a href="{{ '/impressum/' | url }}">Impressum</a>
    </nav>
  </header>

  <main class="rezept-inhalt">
    {{ content | safe }}

    {% if category %}
      {% set categorySlug = category | lower | replace("ä", "ae") | replace("ö", "oe") | replace("ü", "ue") %}
      <p style="text-align: center; margin-top: 2rem;">
        <a href="{{ (categorySlug + '/') | url }}">← Zurück zur Kategorie {{ category }}</a>
      </p>
    {% endif %}
  </main>

  <footer>
    <p>© 2025 Taste Tales – Steve Minder und Florian Zingg</p>
  </footer>
</body>
</html>
